<div id="<%= dom_id room %>" class="mt-6">
  <% if action_name != "show" %>
    <%= link_to "Show room ##{room.id}", room, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
